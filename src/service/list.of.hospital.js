const getListOfHospitalsAndChannels = async () => {

    try {
        // connect to db and get the value from db
        // get the data form the response and store to local database 
    } catch (error) {
       
    }

}